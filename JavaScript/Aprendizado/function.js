function ano(x, y) {
  return(x + y);
}

const anual = ano('Rafael', ' Paulo');

console.log(anual);

// 
function (e) {} // usado para capturar evento
// função    parametro
function    (msg) {}
document.querySelector('...') // seleciona um elemento
const p = document.createElement('p');  // cria um elemnto qualquer
p.classList.add('paragrafo') // acria uma classe no elemento criado
...appendChild(p); // adiciona um filho ao "resultado" (elemento)



